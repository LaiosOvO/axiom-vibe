{
  "name": "@axiom-ai/desktop",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/index.ts --outdir dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "tauri:icon": "tauri icon",
    "ide:build": "bun run ide/build.ts",
    "ide:patch": "bun run ide/patch-brand.ts .build/vscodium && bun run ide/patch-extensions.ts .build/vscodium"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.7",
    "typescript": "5.8.2"
  }
}
